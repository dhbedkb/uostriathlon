.DS_Store

# Build output — rebuilt by .github/workflows/deploy.yml on every push
_site/
.jekyll-cache/
.sass-cache/
# Local backups and one-off migration files
*.backup
*.before-*
*.tmp
*.stage*
admin/*before*
scripts/*before*
*.bak
# Local archive
.archive/
# Local build/cache output
.jekyll-metadata
